{
  "name": "unity-yaml",
  "packageManager": "bun@latest",
  "version": "1.0.0",
  "description": "Fast, token-efficient Unity YAML parser for AI agents",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./cli": "./dist/cli.js",
    "./scanner": "./dist/scanner.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "bun run build:lib && bun run build:cli",
    "build:lib": "bun build src/index.ts --outfile=dist/index.js --target=bun --format=cjs --declaration",
    "build:cli": "bun build src/cli.ts --outfile=dist/cli.js --target=bun --format=cjs",
    "dev": "bun build src/index.ts --watch",
    "clean": "rm -rf dist",
    "rebuild": "bun run clean && bun run build",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage"
  },
  "dependencies": {
    "commander": "^14.0.2"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "@vitest/ui": "^4.0.16",
    "typescript": "^5.3.0",
    "vitest": "^4.0.16"
  },
  "type": "commonjs",
  "bin": {
    "unity-yaml": "./dist/cli.js"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-org/unity-agentic-tools"
  },
  "keywords": [
    "unity",
    "yaml",
    "parser",
    "scene",
    "prefab",
    "gameobject",
    "ai",
    "cli"
  ],
  "license": "Apache-2.0"
}
