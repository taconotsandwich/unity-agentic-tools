## What's Changed

- Refactored the live Unity editor bridge so editor mutations route through a more resilient invoke surface.
- Improved compatibility across multiple Unity versions and hardened bridge recovery and result serialization.
- Added headless validation coverage and stronger integration tests for editor-driven workflows.
- Simplified CLI JSON output and restored `clone` as a top-level command.
