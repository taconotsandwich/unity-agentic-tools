# Rust build artifacts
target/

# napi-rs build output (local .node files in root)
*.node

# Platform package binaries (populated by napi artifacts during CI)
npm/*/*.node

# Node.js
node_modules/
bun.lockb
package-lock.json

# Cargo.lock for libraries
Cargo.lock
